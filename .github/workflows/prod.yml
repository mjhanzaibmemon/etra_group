name: prod Deployment

on:
  push:
    branches: [ "prod" ]
    
jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: prod
    steps:
    - uses: actions/checkout@v4
    - name: SSH Deploy
      run: echo "Example prod deployment"